<p id="notice"><%= notice %></p>

<h1>Records</h1>

<p>適正体重　　BMI22　最も健康的　・　美容体重　　BMI20　見た目がスリム　・　モデル体重　BMI18　モデルレベル</p>

<select id="listbox">
<option value="1">日</option>
<option value="2">週</option>
<option value="3">月</option>
</select>

<canvas id="myChart" width="400" height="100"></canvas>
<script>draw_graph();</script> 

<table>
  <thead>
    <tr>
      <th>Day</th>
      <th>Height</th>
      <th>Weight</th>
      <th>Bmi</th>
      <th>Highblood</th>
      <th>Lowblood</th>
      <th>Diary</th>
      <th>Picture</th>
      <!-- <th>Pictureimage</th> -->
      <th>Targetbmi</th>
      <th>Targetweight</th>
      <th>Compareweight</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @records.each do |record| %>
      <tr>
        <td><%= record.day %></td>
        <td><%= record.height %></td>
        <td><%= record.weight %></td>
        <td><%= record.bmi %></td>
        <td><%= record.highblood %></td>
        <td><%= record.lowblood %></td>
        <td><%= record.diary %></td>
        <!--<td><%= record.picture %></td>-->
        <td>
          <%# <%= debug @record.picture_urls %>
          <% record.picture_urls.each do |picture_url| %>
            <% next if picture_url.blank? %>
            <%= image_tag(picture_url, size: '85x70') %>
          <% end %>
        </td>
        <td><%= record.targetbmi %></td>
        <td><%= record.targetweight %></td>
        <td><%= record.compareweight %></td>
        <td><%= link_to 'Show', record %></td>
        <td><%= link_to 'Edit', edit_record_path(record) %></td>
        <td><%= link_to 'Destroy', record, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<%= link_to 'New Record', new_record_path %>

<script>
  set_handler();
</script>
